import { GLOBAL_COLOR, FONT_SIZE } from "../../../../themes";

const detailedContactStyle = {
  textHistory: {
    color: GLOBAL_COLOR.TEXT_HEADER,
    fontSize: FONT_SIZE.TITLE
  }
};

export { detailedContactStyle };
